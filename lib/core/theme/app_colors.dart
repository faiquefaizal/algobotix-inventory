import 'package:flutter/material.dart';

/// Defines the color palette used throughout the application.
///
/// Centralizes all color definitions to ensure consistency.
class AppColors {
  /// The primary brand color (Teal).
  static const Color primary = Colors.teal;
  static const Color primaryLight = Color(0xFFE0F2F1);

  static const Color white = Colors.white;
  static const Color black = Colors.black87;
  static const Color grey = Colors.grey;
  static const Color lightGrey = Color(0xFFEEEEEE);
  static const Color borderGrey = Color(0xFFE0E0E0);

  /// Color used for error states and validation messages.
  static const Color error = Colors.red;

  static const Color iconColor = Colors.grey;
}
